apiVersion: v1
kind: Service
metadata:
  name: support-ticket-api-service
  namespace: default
spec:
  selector:
    app: support-ticket-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
